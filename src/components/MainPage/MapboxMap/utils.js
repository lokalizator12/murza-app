export const applyFilters = (request, filters) => {
   /* if (filters.maxWeight && request.weight > filters.maxWeight) return false;
    return !(filters.maxVolume && request.volume > filters.maxVolume);*/
    return true;
};
