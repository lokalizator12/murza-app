import React from 'react';
import MyMap from '../components/Map';

const AppMap = () => {
    return (
        <div>
            <h1>Mapbox </h1>
            <MyMap/>
        </div>
    );
};

export default AppMap;
